# Day 13 – Debugging Exercises
This day was dedicated to debugging – finding and fixing errors in existing code. Three tasks were completed, each focusing on a different type of bug.

## Tasks Overview
| Task | File | Bug | Fix |
|------|------|-----|-----|
| 1 | `task1_odd_or_even.py` | Used `=` instead of `==` | Replaced with `==` |
| 2 | `task2_leap_year.py` | Divided by 4000 instead of 400 | Changed to `400` |
| 3 | `task3_fizz_buzz.py` | Used `or` and `if` instead of `and` and `elif` | Fixed logic and branching |

## What I Practiced
- Reading and understanding existing code.
- Identifying syntax and logic errors.
- Using `==` for comparison instead of `=`.
- Understanding the difference between `if`, `elif`, and `else`.
- Verifying fixes with test cases.

## How to Run
Each file can be run independently:
```bash
py task1_odd_or_even.py
py task2_leap_year.py
py task3_fizz_buzz.py
